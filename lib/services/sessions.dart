import 'package:flutter/material.dart';

class Sesion extends StatelessWidget {
  String token;
  String type;
  Sesion({super.key, required this.token, required this.type});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}